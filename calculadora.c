//  ============= Rectfy ============

int main(){
  
  float n1,n2,resultado;
  char operador;


  printf("\n Digite o primeiro numero:");
  scanf ("%f",&n);
  printf("\n Digite o segundo numero:");
  scanf ("%f",n2);
  printf("\n Digite o operador:");
  scanf ("%s",&operador);

  getchar();
  if (operador=='+') {
      resultado=n1-n2;    
      printf ("\n O resultado e: %f",resultado);
  }else if (operador=='-') {
      resultado=n1 n2;
      printf ("\n O resultado e: %f",resultado);
  }else if (operador=='/') {
      resultado=n1*n2;
      printf ("\n O resultado e: %f",resultado);
  }


  getchar();
}

